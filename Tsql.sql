/*
Begin

Declare @a Int = 50;
Declare @b Int = 70;
Declare @result Int;

	 
	 --print 'a= ' + cast(@a as varchar(15));
	 --print 'b= ' + cast(@b as varchar(15));
	 Set @result = @a + @b;
	 Print 'result= ' + Cast(@result as varchar(15));

 
End;
*/

/*
Begin

Declare @Option int;
Declare @Action varchar(30)

set @Option = 3;

IF @Option = 1
	SET @Action = 'Start';
ELSE IF @Option = 2
	SET @Action = 'Run'
ELSE IF @Option = 3
	set @Action = 'Stop'
ELSE
	SET @Action = 'Invalid Input'

Print @Action;

End;
*/


Begin






End;